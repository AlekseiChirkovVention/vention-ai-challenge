Path to task description.md: $ARGUMENTS

You are executing the **pv-finalize** workflow — Validation Step 6 (Finalization: update state files, record deferred tasks, compress).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pv-finalize.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

When instructed to call `/compress-state`, follow `.windsurf/workflows/compress-state.md` with the `--auto` flag.

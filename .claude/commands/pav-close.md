Path to T-XX/description.md: $ARGUMENTS

You are executing the **pav-close** workflow — Atomic Validation Step 3 (Close task, set status to reviewed).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pav-close.md` and follow ALL instructions exactly, using the path above as `<path-to-atomic-description>`.

When instructed to call `/compress-state`, follow `.windsurf/workflows/compress-state.md` with the `--auto` flag.

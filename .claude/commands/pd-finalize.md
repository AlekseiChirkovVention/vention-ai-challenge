Path to task description.md: $ARGUMENTS

You are executing the **pd-finalize** workflow — Discovery Step 4 (Finalization).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pd-finalize.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

When instructed to call `/compress-state`, follow `.windsurf/workflows/compress-state.md` with the `--auto` flag.

Path to task description.md: $ARGUMENTS

You are executing the **pd-decompose** workflow — Discovery Step 2 (Decomposition into T-XX subtasks).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pd-decompose.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

Decomposition rules are defined in `.windsurf/rules/workflow-globals.md` under `## Decomposition Rules`.

Path to task description.md: $ARGUMENTS

You are executing the **pv-contract** workflow — Validation Step 1 (Contract verification: Invariants, Acceptance Criteria, Off-limits).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pv-contract.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

IMPORTANT: If any Invariant or Off-limits constraint FAILS — stop immediately, list failures, ask user to choose: (1) pause and fix, or (2) continue with failures marked critical.

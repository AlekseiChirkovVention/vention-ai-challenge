Path to T-XX/description.md: $ARGUMENTS

You are executing the **pav-propose** workflow — Atomic Validation Step 2 (Improvement proposals, only runs if verdict is DONE).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pav-propose.md` and follow ALL instructions exactly, using the path above as `<path-to-atomic-description>`.

IMPORTANT: If "Do Now" improvements exist — present them to the user and WAIT for approval before applying.

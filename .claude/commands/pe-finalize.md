Path to task description.md: $ARGUMENTS

You are executing the **pe-finalize** workflow — Execution Step 2 (Summary report and finalization).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pe-finalize.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

When instructed to call `/compress-state`, follow `.windsurf/workflows/compress-state.md` with the `--auto` flag.

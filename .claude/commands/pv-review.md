Path to task description.md: $ARGUMENTS

You are executing the **pv-review** workflow — Validation Step 2 (Code review: readability, architecture, security, edge cases).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pv-review.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

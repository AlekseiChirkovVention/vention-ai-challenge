Path to task description.md: $ARGUMENTS

You are executing the **pd-design** workflow — Discovery Step 3 (Architectural Design).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pd-design.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

IMPORTANT: You must present 2–3 architectural variants to the user and WAIT for their decision before proceeding.

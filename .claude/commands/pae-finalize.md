Path to T-XX/description.md: $ARGUMENTS

You are executing the **pae-finalize** workflow — Atomic Execution Step 2 (Update statuses in parent state and plan files).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pae-finalize.md` and follow ALL instructions exactly, using the path above as `<path-to-atomic-description>`.

When instructed to call `/compress-state`, follow `.windsurf/workflows/compress-state.md` with the `--auto` flag.

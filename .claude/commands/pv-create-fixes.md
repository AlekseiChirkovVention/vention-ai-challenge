Path to task description.md: $ARGUMENTS

You are executing the **pv-create-fixes** workflow — Validation Step 4 (Create fix-task subtask directories from approved issues).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pv-create-fixes.md` and follow ALL instructions exactly, using the path above as `<path-to-description>`.

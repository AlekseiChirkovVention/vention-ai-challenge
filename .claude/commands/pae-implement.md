Path to T-XX/description.md: $ARGUMENTS

You are executing the **pae-implement** workflow — Atomic Execution Step 1 (Execute implementation steps).

Read `.windsurf/rules/workflow-globals.md` for global principles, then read `.windsurf/workflows/pae-implement.md` and follow ALL instructions exactly, using the path above as `<path-to-atomic-description>`.

IMPORTANT:
- Update the parent `## Subtask Progress` block after EVERY completed step, not just at the end.
- Stop immediately and report to user on any blocker or failed verification.
